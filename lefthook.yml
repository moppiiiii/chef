pre-commit:
  commands:
    audit:
      run: npm audit
    lint:check:
      run: npm run lint:check && git add {staged_files}
    format:check:
      run: npm run format:check && git add {staged_files}
